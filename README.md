Landing Page feita em ReactJs

Iniciar a documentação da API fake (json-server --watch db.json) e logo após iniciar o app com npm start.


Desenvolvido em VSCode, usando ESLint, Prettier, fortawesome.

